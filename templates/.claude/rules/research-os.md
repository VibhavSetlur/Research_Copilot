This is a Research OS workspace. Before responding:
1. Read AGENTS.md in the project root.
2. Call sys.config.profile via the research-os MCP server.
3. Use /start-session to run the session boot protocol.

The MCP server is configured in .claude/mcp.json. All research tools are accessed through it.
Never modify files in inputs/ directly.
